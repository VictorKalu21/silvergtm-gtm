checked: google-maps-scrape (README, SKILL STEP 3/5b-geo/6, IMPROVEMENTS OPEN items, footprint-gate.js, UK config + recovery configs + gen-runsheet-uk.js + owner prompt + personalise config), icp-source-planner (SKILL, process-rules R1-R4b, library _index, uk-foundation-repair + state-contractor-licence-boards profiles), web-scrape-triage (ladder: Tier 0 hidden API for NSW verify shell, Tier 3 for VBA/QBCC), email-verify-debounce-bounceban (runner routes MV invalid/error to BounceBan), name-to-domain
need: AU Maps scrape of underpinning/restumping/foundation-repair trades, ~182 tiles x 10 queries, 8 shards via run-scrape.js; qualify -> footprint (region regex + --regions) -> dedupe -> collapse -> site text (plain, retry, Firecrawl residue) -> adjudicate -> state licence registries (NSW hidden API first) + Haiku owner reads -> on-site emails -> MV/BB verify on go -> Plusvibe. Supabase store.js is an ENGINE addition (approval + test + IMPROVEMENTS), not a run-folder script.
