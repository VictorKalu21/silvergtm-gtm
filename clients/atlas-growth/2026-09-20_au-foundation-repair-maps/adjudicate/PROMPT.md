You are qualifying Australian building-trade businesses for a cold-outreach list whose ICP is **foundation repair** — companies that REPAIR existing houses' foundations and structural movement: underpinning (mass concrete, screw pile, bored pier, resin/polyurethane injection, geopolymer), **restumping and reblocking** (replacing the timber stumps under a stumped house with concrete or steel — the Victorian and Tasmanian word for the same job), relevelling and house levelling, slab lifting / slab jacking, subsidence and footing repair, crack stitching and structural repairs to a moving house, and ground stabilisation delivered as a contracted service to homeowners.

You get one JSONL file: one business per line with `name`, `google_types`, `city`, `tier` (a keyword pre-label), `matched_terms`, `evidence` (keyword hit snippet), and `text` (~2,500 chars from the business's own website, services page first).

For EACH line, decide from the TEXT (not the keyword label) and output one JSON object with:
- `place_id` (copy exactly)
- `offers_foundation_repair`: "yes" | "no" | "unclear"
   yes  = the business itself carries out foundation / stump / footing REPAIR work on existing houses as an offered service (underpinning, restumping, reblocking, relevelling, slab lifting, resin injection, subsidence or footing repair, structural crack repair, ground stabilisation) — even if it is one of several services. A franchise licensee that delivers the work locally (Mainmark, Buildfix, Uretek, Teretek branches) is "yes".
   no   = the terms are incidental (a verb like "underpinned by our values"; a supplier or manufacturer of stumps, piers, resin or equipment; an engineer, inspector or certifier who only designs or inspects; a builder or concreter that only lays NEW footings, slabs or driveways; a bathroom / balcony waterproofer; a removalist that answers "house levelling"; a pest or termite firm; a franchise head office with no service delivery; or the text is empty / unrelated).
   unclear = the text is too thin to tell.
- `bucket`: one of "underpinning_restumping" | "slab_lifting_levelling" | "structural_repair_specialist" | "house_raising" | "groundworks_new_foundations" | "engineer_inspector" | "supplier_manufacturer" | "general_builder" | "waterproofing_other" | "other"
   (pick the business's PRIMARY identity; a house-raising firm that also restumps = "house_raising" with offers_foundation_repair "yes" if it genuinely offers restumping / underpinning / relevelling as a service; a general builder with a real restumping line = "general_builder" with "yes").
- `reason`: ≤ 25 words quoting the decisive phrase from the text.

Rules: judge only from the supplied text; never infer a service that isn't stated; a bare menu item like "Restumping" or "Underpinning" in a services list counts as offered. Australian spelling in the text is normal (levelling, stabilisation). Do the work YOURSELF — do NOT spawn, launch, or delegate to other agents. You personally write the output file: a single JSON array (no BOM, no markdown fences) to the output path given, one object per input line, same order. Write the file, then reply with just the counts (yes/no/unclear).
